DSPACE_VER=dspace-8.1
DOCKER_OWNER=dspace
DOCKER_REGISTRY=docker.io
COMPOSE_PROJECT_NAME=dare-institutional-repository

# Database password — CHANGE THIS to a strong random value before first run.
# Generate one with: openssl rand -base64 32
# Keep this file out of version control (it is listed in .gitignore).
POSTGRES_PASSWORD=BPoly2026Secure!
