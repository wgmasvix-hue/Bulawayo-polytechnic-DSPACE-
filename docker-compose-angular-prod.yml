services:
  dspace-angular:
    image: dspace/dspace-angular:dspace-8.1-dist
    container_name: dspace-angular
    restart: unless-stopped

    environment:
      # UI server (Node.js) listens on all interfaces inside the container.
      # nginx terminates TLS externally and proxies to port 4000.
      DSPACE_UI_SSL: "false"
      DSPACE_UI_HOST: "0.0.0.0"
      DSPACE_UI_PORT: "4000"
      DSPACE_UI_NAMESPACE: "/"

      # REST API endpoint — must be the PUBLIC hostname that end-user browsers
      # can resolve.  Using the internal Docker hostname ('dspace') here would
      # cause browser-side JavaScript to fail after server-side rendering hands
      # off to the client.
      DSPACE_REST_SSL: "true"
      DSPACE_REST_HOST: "bulawayo-polytechnic-repository.dare.co.zw"
      DSPACE_REST_PORT: "443"
      DSPACE_REST_NAMESPACE: "/server"

    volumes:
      - ./config.yml:/app/config/config.yml:ro

    # Bind Angular to loopback only — nginx forwards external HTTPS traffic.
    ports:
      - "127.0.0.1:4000:4000"

    depends_on:
      - dspace

    networks:
      - dspacenet

networks:
  dspacenet:
    external: true
    name: bulawayo-polytechnic-dspace-official_dspacenet
